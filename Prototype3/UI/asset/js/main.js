
    // Function to open the delete modal and set the stagiaire ID
    function openDeleteModal(id) {
        const confirm_Delete = document.getElementById('confirm_Delete');
        confirm_Delete.value = id;
    }
